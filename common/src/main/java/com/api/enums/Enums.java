package com.api.enums;

public class Enums {
    public enum Customer{
        id,
        name,
        email
    }

    public enum Product {
        id,
        name,
        price
    }

    public enum Order{
        id,
        customerId,
        orderDate
    }
}
